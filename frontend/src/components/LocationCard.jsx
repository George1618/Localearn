import { View } from "react-native";

export default function LocationCard({ location }) {
    return (<View>
        {/* TODO (OPCIONAL): mostrar os dados de location. Primeiro definir o location */}
    </View>)
}